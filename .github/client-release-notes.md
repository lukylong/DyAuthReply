## D助手 0.1.28

- 修正账号卡片状态语义：快捷登录完成并通过身份、租约与接收链路校验后显示“账号正常”，不再把尚无发送回执误显示为“待检测”。
- 发送能力仍由真实平台回执独立判定；登录失效、发送封控和仅接收状态继续明确展示，不以接收在线冒充可发送。
- 补齐作品接口要求的 Rust 原生 `x-secsdk-web-signature` URL 签名，并对瞬时 HTTP 403 最多进行两次有界退避重试，修复作品列表拉取失败。
- 保持 Rust 原生运行时、快捷登录、消息协议、macOS/Windows 安装和自动更新链路。
